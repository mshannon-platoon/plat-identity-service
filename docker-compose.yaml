version: '3.1'
services:
  postgres:
    image: postgres:13.3-alpine
    ports:
      - "5432:5432"
    expose:
      - 5432
    environment:
      POSTGRES_DB: user_store
      POSTGRES_USER: root
      POSTGRES_PASSWORD: password